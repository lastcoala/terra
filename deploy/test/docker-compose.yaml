name: terra-test

services:
  postgres:
    restart: no
    image: postgres:16
    environment:
      - POSTGRES_USER=terra
      - POSTGRES_PASSWORD=terra123
      - POSTGRES_DB=terra
    ports:
      - 5433:5432